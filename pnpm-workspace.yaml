# pnpm 10+ blocks lifecycle scripts unless the package is whitelisted here.
# better-sqlite3 fetches/builds its native binding, esbuild fetches its binary,
# without these two the smoke test & the build both fail at runtime.
onlyBuiltDependencies:
  - better-sqlite3
  - esbuild
